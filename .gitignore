__pycache__/
*.pyc
.env
logs/*.log
instance/
# Raw API cache from scripts/fetch_anime_catalog.py (regenerable)
anime_catalog_raw.json
anime_official_images.json
anime_streaming.json
anime_schedule.json
# Transient MAL episode-fetch worker caches (resume state lives in the base cache + todo)
anime_mal_episodes_w*.json
# Transient TVmaze thumbnail-fetch worker caches (resume state lives in the base cache + todo)
anime_ep_thumbs_w*.json
# Runtime SQLite database (dev fallback only — production uses Turso)
animechat.db
